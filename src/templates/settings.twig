{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * Craft Cognito Auth plugin for Craft CMS 3.x
 *
 * Craft Cognito Auth Settings.twig
 *
 * @author    Mike Pierce
 * @copyright Copyright (c) 2019 Mike Pierce
 * @link      https://edenspiekermann.com
 * @package   CraftCognitoAuth
 * @since     0.1.0
 */
#}

{% import "_includes/forms" as forms %}

{{ forms.lightswitchField({
    label: 'Auto create user',
    instructions: 'If enabled, will automatically create a public user when provided with a verified JWT.',
    suggestEnvVars: true,
    id: 'autoCreateUser',
    name: 'autoCreateUser',
    on: settings['autoCreateUser'] is defined? settings['autoCreateUser'] : false})
}}

{{ forms.autosuggestField({
    label: 'AWS Cognito region',
    instructions: 'Enter your AWS Cognito region',
    suggestEnvVars: true,
    id: 'region',
    name: 'region',
    value: settings['region'] is defined? settings['region'] : ''})
}}

{{ forms.autosuggestField({
    label: 'AWS Cognito app client id',
    instructions: 'Enter your AWS Cognito app client id (under App integration -> app client settings)',
    suggestEnvVars: true,
    id: 'clientId',
    name: 'clientId',
    value: settings['clientId'] is defined? settings['clientId'] : ''})
}}

{{ forms.autosuggestField({
    label: 'AWS Cognito app client id',
    instructions: 'Enter your AWS Cognito app client id (under App integration -> app client settings)',
    suggestEnvVars: true,
    id: 'clientSecret',
    name: 'clientSecret',
    value: settings['clientSecret'] is defined? settings['clientSecret'] : ''})
}}

{{ forms.autosuggestField({
    label: 'AWS Cognito user pool id',
    instructions: 'Enter your AWS Cognito user pool id (under General settings)',
    suggestEnvVars: true,
    id: 'userpoolId',
    name: 'userpoolId',
    value: settings['userpoolId'] is defined? settings['userpoolId'] : ''})
}}

{{ forms.autosuggestField({
    label: 'JSON Web Key Set URL',
    instructions: 'Enter your JSON Web Key Set URL (JWKS), used for verifying incoming Cognito JWTs',
    suggestEnvVars: true,
    id: 'jwks',
    name: 'jwks',
    value: settings['jwks'] is defined? settings['jwks'] : ''})
}}

{{ forms.autosuggestField({
    label: 'SAML Certificate',
    instructions: 'Enter your SAML Certificate, used for verifying incoming SAML messages',
    suggestEnvVars: true,
    id: 'samlCert',
    name: 'samlCert',
    value: settings['samlCert'] is defined? settings['samlCert'] : ''})
}}

{{ forms.autosuggestField({
    label: 'SAML Login URL',
    instructions: 'Enter the SAML IdP login URL',
    suggestEnvVars: true,
    id: 'samlIdPLogin',
    name: 'samlIdPLogin',
    value: settings['samlIdPLogin'] is defined? settings['samlIdPLogin'] : ''})
}}